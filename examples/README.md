# Examples

This directory contains sample layouts for testing onboarding and future integration flows.

## `sample-spec-repo/`

A specimen repository structure that mirrors expected production layout:
- `specs/` for source-of-truth markdown specs
- `specforge/` for overlay metadata and tool-specific files

Use this directory to prototype ingest flows without modifying real project repositories.
