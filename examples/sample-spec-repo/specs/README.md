# Sample Spec Repo / specs

This placeholder mirrors where source spec files would live in an external repository.

For v0.1, use the root `/specs` directory as primary implementation target. This sample layout exists to support future adapter and ingestion testing.
