# Sample Spec Repo / specforge

This placeholder mirrors where overlay metadata and SpecForge runtime files would live in an external repository.

Future integration tests can copy or symlink root-level example overlay files into this location.
