# Add `/api/dashboard/overview`, `/api/market-data/candles`, `/api/strategies/candidates`, `/api/backtests/runs`, and `/api/backtests/runs/{runKey}`.

## ID
T-0604

## Type
Task

## Parent
S-0601

## Summary
Add `/api/dashboard/overview`, `/api/market-data/candles`, `/api/strategies/candidates`, `/api/backtests/runs`, and `/api/backtests/runs/{runKey}`.

## Problem / Context
Migrated from legacy-specs/0006-dashboard-read-model-mvp/tasks.md.

## Goals
- Add `/api/dashboard/overview`, `/api/market-data/candles`, `/api/strategies/candidates`, `/api/backtests/runs`, and `/api/backtests/runs/{runKey}`.

## Non-goals
- Do unrelated work outside the migrated checklist item.

## Requirements
- Complete the migrated task: Add `/api/dashboard/overview`, `/api/market-data/candles`, `/api/strategies/candidates`, `/api/backtests/runs`, and `/api/backtests/runs/{runKey}`.

## Acceptance Criteria
- The migrated task is represented as a canonical SpecForge task with a valid story parent.

## Dependencies
- S-0601

## Open Questions
- None

## Notes
- Original task text: Add `/api/dashboard/overview`, `/api/market-data/candles`, `/api/strategies/candidates`, `/api/backtests/runs`, and `/api/backtests/runs/{runKey}`.
