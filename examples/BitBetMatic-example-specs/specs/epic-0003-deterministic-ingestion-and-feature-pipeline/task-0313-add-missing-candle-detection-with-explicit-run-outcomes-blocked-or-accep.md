# Add missing-candle detection with explicit run outcomes (`blocked` or `accepted_with_gap_markers`).

## ID
T-0313

## Type
Task

## Parent
S-0301

## Summary
Add missing-candle detection with explicit run outcomes (`blocked` or `accepted_with_gap_markers`).

## Problem / Context
Migrated from legacy-specs/0003-deterministic-ingestion-and-feature-pipeline/tasks.md.

## Goals
- Add missing-candle detection with explicit run outcomes (`blocked` or `accepted_with_gap_markers`).

## Non-goals
- Do unrelated work outside the migrated checklist item.

## Requirements
- Complete the migrated task: Add missing-candle detection with explicit run outcomes (`blocked` or `accepted_with_gap_markers`).

## Acceptance Criteria
- The migrated task is represented as a canonical SpecForge task with a valid story parent.

## Dependencies
- S-0301

## Open Questions
- None

## Notes
- Original task text: Add missing-candle detection with explicit run outcomes (`blocked` or `accepted_with_gap_markers`).
