# Add batch-level ingestion observability (batch index, requested window, fetched/inserted/skipped counts, empty or unexpectedly small responses).

## ID
T-0309

## Type
Task

## Parent
S-0301

## Summary
Add batch-level ingestion observability (batch index, requested window, fetched/inserted/skipped counts, empty or unexpectedly small responses).

## Problem / Context
Migrated from legacy-specs/0003-deterministic-ingestion-and-feature-pipeline/tasks.md.

## Goals
- Add batch-level ingestion observability (batch index, requested window, fetched/inserted/skipped counts, empty or unexpectedly small responses).

## Non-goals
- Do unrelated work outside the migrated checklist item.

## Requirements
- Complete the migrated task: Add batch-level ingestion observability (batch index, requested window, fetched/inserted/skipped counts, empty or unexpectedly small responses).

## Acceptance Criteria
- The migrated task is represented as a canonical SpecForge task with a valid story parent.

## Dependencies
- S-0301

## Open Questions
- None

## Notes
- Original task text: Add batch-level ingestion observability (batch index, requested window, fetched/inserted/skipped counts, empty or unexpectedly small responses).
