# Enforce idempotent candle persistence semantics in `MarketCandleRecord` keyed by deterministic candle identity.

## ID
T-0308

## Type
Task

## Parent
S-0301

## Summary
Enforce idempotent candle persistence semantics in `MarketCandleRecord` keyed by deterministic candle identity.

## Problem / Context
Migrated from legacy-specs/0003-deterministic-ingestion-and-feature-pipeline/tasks.md.

## Goals
- Enforce idempotent candle persistence semantics in `MarketCandleRecord` keyed by deterministic candle identity.

## Non-goals
- Do unrelated work outside the migrated checklist item.

## Requirements
- Complete the migrated task: Enforce idempotent candle persistence semantics in `MarketCandleRecord` keyed by deterministic candle identity.

## Acceptance Criteria
- The migrated task is represented as a canonical SpecForge task with a valid story parent.

## Dependencies
- S-0301

## Open Questions
- None

## Notes
- Original task text: Enforce idempotent candle persistence semantics in `MarketCandleRecord` keyed by deterministic candle identity.
