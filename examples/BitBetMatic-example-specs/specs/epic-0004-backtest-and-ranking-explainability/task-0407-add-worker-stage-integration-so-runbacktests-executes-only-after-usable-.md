# Add worker-stage integration so `RunBacktests` executes only after usable ingestion/feature stages.

## ID
T-0407

## Type
Task

## Parent
S-0401

## Summary
Add worker-stage integration so `RunBacktests` executes only after usable ingestion/feature stages.

## Problem / Context
Migrated from legacy-specs/0004-backtest-and-ranking-explainability/tasks.md.

## Goals
- Add worker-stage integration so `RunBacktests` executes only after usable ingestion/feature stages.

## Non-goals
- Do unrelated work outside the migrated checklist item.

## Requirements
- Complete the migrated task: Add worker-stage integration so `RunBacktests` executes only after usable ingestion/feature stages.

## Acceptance Criteria
- The migrated task is represented as a canonical SpecForge task with a valid story parent.

## Dependencies
- S-0401

## Open Questions
- None

## Notes
- Original task text: Add worker-stage integration so `RunBacktests` executes only after usable ingestion/feature stages.
