# Define deterministic backtest run schema (dataset refs, params, window, cost assumptions).

## ID
T-0401

## Type
Task

## Parent
S-0401

## Summary
Define deterministic backtest run schema (dataset refs, params, window, cost assumptions).

## Problem / Context
Migrated from legacy-specs/0004-backtest-and-ranking-explainability/tasks.md.

## Goals
- Define deterministic backtest run schema (dataset refs, params, window, cost assumptions).

## Non-goals
- Do unrelated work outside the migrated checklist item.

## Requirements
- Complete the migrated task: Define deterministic backtest run schema (dataset refs, params, window, cost assumptions).

## Acceptance Criteria
- The migrated task is represented as a canonical SpecForge task with a valid story parent.

## Dependencies
- S-0401

## Open Questions
- None

## Notes
- Original task text: Define deterministic backtest run schema (dataset refs, params, window, cost assumptions).
